# Godot 4+ specific ignores
# .godot/ # this caused laptop UIDs to mismatch, chatgpt said this was bad, but still ignore these:
.godot/shader_cache/
.godot/editor/
.godot/imported/

# (idk where .import will exist)
.import/

# Godot-specific ignores (i mean, so were the above?)
export.cfg
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores (i guess if i use c#)
.mono/
data_*/
mono_crash.*.json

### suggested by chatgpt:

# OS/system files (putting the comments to the right messes it up?)
# macOS
# Windows
# Windows
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log

# Builds
/build/
bin/
# Custom export folder (rename if needed)
export/
*.exe
*.pck
*.apk
*.xcodeproj
*.xcworkspace
